<div class="album text-muted">
  <div class="container">
    <div class="row">
      <!--  <%= link_to "Create new Item", new_project_path  if logged_in?(:site_admin) %> -->
        <%= render @projects %>
    </div>
  </div>
</div>
